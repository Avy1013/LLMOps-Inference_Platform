# Dockerfile.credits
FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi "uvicorn[standard]"

COPY credit_service.py .

# Expose the port the app runs on
EXPOSE 8000

# Run the application
CMD ["uvicorn", "credit_service:app", "--host", "0.0.0.0", "--port", "8000"]